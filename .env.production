# Frontend environment variables for production build
# These are compiled at build time

# API URL - Railway will inject this via environment variables
# If deploying on Railway, this is automatically set
# If deploying separately, update this to your backend URL
VITE_API_URL=https://goldstone-api.railway.app

# Environment
VITE_ENV=production
